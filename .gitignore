# Sensitive files
certs/
server.key
server.key.pub

# Data and logs
data/*
!data/.gitkeep
honeypots/data/
*.log

# Docker overrides
docker-compose.override.yml

# HTTP classifier checkpoint (db-specific runtime state)
extras/tests/http/last_checked_id.txt

# three-globe bug repro
extras/three-globe-bug/
static/three-globe-leak-repro.html
static/ip-blocklist-*.txt

# local analysis scripts
extras/graphs/

# Python
__pycache__/
.venv/
*.pyc
.env

# Local agent/editor state
.codex
.claude/settings.local.json

# Per-server deployment scripts (contain server IPs)
allow-feeders.sh

# Aggregator SMTP-body-backfill fleet identity file (contains every server's IP/host/domain)
fleet.txt
fleet*.txt

# Local protocol customizations (never tracked upstream)
extensions.py
